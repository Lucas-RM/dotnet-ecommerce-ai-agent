export const environment = {
  production: false,
  apiUrl: 'http://localhost:5149/api/v1',
  /** Base URL do ECommerce.AgentAPI (JWT repassado pelo authInterceptor). */
  agentApiUrl: 'http://localhost:5200'
};
