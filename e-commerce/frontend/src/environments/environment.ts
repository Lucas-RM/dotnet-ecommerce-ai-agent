export const environment = {
  production: false,
  apiUrl: 'http://localhost:5149/api/v1',
  /** POST do widget do Agent (JWT repassado pelo authInterceptor). */
  agentApiUrl: 'http://localhost:5200'
};
